# Example environment variables for local development and CI
# Copy to .env and fill with your project's values. Do NOT commit .env with real secrets.

# Firebase client configuration (public keys)
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_DATABASE_URL=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=
FIREBASE_MEASUREMENT_ID=

# Optional: any other env keys your deployment uses
#NODE_ENV=development
